server:
  port: 8099
eureka:
  client:
    register-with-eureka: false
    service-url:
      defaultZone: http://localhost:8761/eureka
spring:
    gateway:
      routes:
      - predicates:
        - Path=/micro-crud/**
        filters:
        - StripPrefix=1
        uri: "http://localhost:8080"
